stages:
  - build
  - test
  
build-backend: 
  image: gradle
  stage: build
  script:
    - .\gradlew clean install 
  artifacts:
    paths: 
      - backend/target/**
    when: always
    expire_in: 2 hours
      
