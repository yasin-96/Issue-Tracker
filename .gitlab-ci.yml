stages:
  - build
  - test
  
build-backend: 
  tags: 
    - java
  stage: build
  script:
    - gradle clean install 
  artifacts:
    paths: 
      - backend/target/**
    when: always
    expire_in: 2 hours
      
