stages:
  - build
  - test
  
build-backend: 
  image: gradle:latest
  stage: build
  script:
    - .\gradlew clean install 
  artifacts:
    paths: 
      - backend/target/**
    when: always
    expire_in: 2 hours
      